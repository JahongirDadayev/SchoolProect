package com.example.restfulproject;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class DemoApplication {
    /*
         Spring project orqali CRUD (Create, Read, Update, Delete) prinsiplari asosida maktab strukturasini quring,
         bunda kamida School, Student, Teacher, Subject, Mark, Groups, TimeTable kabi class ma'lumotlari bo'lsin
         (projectga oid qo'shimcha class lar qo'shish ixtiyoriy).
    */
    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
    }

}
